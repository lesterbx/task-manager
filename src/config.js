const db = {
  protocol: 'http',
  host: 'localhost',
  port: 5984
}
const dbAdmin = {
  user: 'lester',
  password: 'mono12'
}
const server = {
  protocol: 'http',
  host: 'localhost',
  port: 3000
}

export { db }
export { dbAdmin }
export { server }
